interface VacationModel {
    vacationId: number;
    destination: string;
    description: string;
    image: FileList;
    imageName: string;
    startDate: string;
    endDate: string;
    price: number;
}

export default VacationModel;

